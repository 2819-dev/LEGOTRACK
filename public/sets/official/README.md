# Official LEGO set images (required)

Put **real catalog / product PNGs with true transparent backgrounds** here.

Examples of what we need:
- One set per file (Fire Station, Police Car, Creator house, etc.)
- PNG with alpha (checkerboard background in Preview / Photoshop)
- Prefer the built model floating alone — not box art with a photo background

Then tell the agent the filenames (or just say “wire up whatever is in this folder”) and the splash will fade between them.

**Do not** use random photos with backgrounds roughly erased — those look wrong.
