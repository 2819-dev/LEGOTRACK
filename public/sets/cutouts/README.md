# Transparent Lego set cutouts for the splash carousel
#
# Drop official / catalog PNGs with transparent backgrounds here.
# Name them anything ending in .png — then add the path to SETS in
# src/components/SplashScreen.tsx (or ask the agent to wire them in).
#
# Current files were auto-cut from photos; replace with better ones anytime.
